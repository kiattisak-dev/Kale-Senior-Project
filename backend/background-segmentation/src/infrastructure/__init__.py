from .model_loader import ModelLoader

# Initialize a global instance of the ModelLoader
model_loader = ModelLoader()

# This makes model_loader available for import as:
# from src.infrastructure import model_loader
